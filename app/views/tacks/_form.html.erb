<%= form_for @tack, html: { multipart: true } do |f| %>
  <% if @tack.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tack.errors.count, "error") %> prohibited this tack from being saved:</h2>

      <ul>
      <% @tack.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :image %>
    <%= f.file_field :image, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :description %>
    <%= f.text_field :description, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.submit class: "btn btn-primary" %>
  </div>
<% end %>